<main class="home">
  <h1>Productos</h1>

  <div class="card-container">
    {{#if products.length}}
    {{#each products}}
      <div class="card">        
        <h2>{{this.title}}</h2>
        <p>{{this.description}}</p>
        <div><p>${{this.price}} - Stock: {{this.stock}}</p></div>
        </div>
    {{/each}}
    {{else}}
    <h2>No hay ningun productos</h2>
{{/if}}
  </div>

</main>